# 小麦拼豆 Beta

小麦拼豆是一款在浏览器中运行的拼豆图纸设计器，支持图片转拼豆图、格子编辑、颜色替换、选区、钢笔、参考图、项目保存以及 PDF/PNG 导出。

## 隐私

图片转换和图纸编辑默认在浏览器本地完成。上传的图片不会主动发送到服务器。

## 本地运行

在项目根目录运行：

```bash
npm install
npm run dev
```

## 发布

`main` 分支更新后，GitHub Actions 会把 `public/` 自动发布到 GitHub Pages。
